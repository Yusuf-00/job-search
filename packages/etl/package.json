{
  "name": "@job-search/etl",
  "private": true,
  "scripts": {
    "load": "DOTENV_CONFIG_PATH=../../.env ts-node -r dotenv/config src/load-postgres.ts",
    "augment": "DOTENV_CONFIG_PATH=../../.env ts-node -r dotenv/config src/augment-dataset.ts",
    "index": "DOTENV_CONFIG_PATH=../../.env ts-node -r dotenv/config src/index-meilisearch.ts",
    "seed": "npm run load && npm run augment && npm run index"
  },
  "dependencies": {
    "pg": "^8.12.0",
    "csv-parse": "^5.5.6",
    "meilisearch": "^0.44.1",
    "dotenv": "^16.4.5"
  },
  "devDependencies": {
    "ts-node": "^10.9.2",
    "typescript": "^5.5.4",
    "@types/node": "^20.14.0",
    "@types/pg": "^8.11.6"
  }
}